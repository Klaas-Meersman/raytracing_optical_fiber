{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build CMake Project",
            "type": "shell",
            "command": "cmake --build ${workspaceFolder}/build",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "detail": "Task to build the entire project using CMake."
        }
    ]
}
